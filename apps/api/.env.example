# همه‌چیز اختیاری است؛ تنظیمات اصلی (پیامک، کلینیک، ...) از پنل مدیریت انجام می‌شود.
# مسیر دیتابیس (پیش‌فرض SQLite داخل پوشه data)
DATABASE_URL="file:../data/clinic.db"
# پورت سرور API
PORT=4000
# آدرس فرانت برای CORS (در حالت توسعه لازم نیست)
WEB_ORIGIN=http://localhost:3000
